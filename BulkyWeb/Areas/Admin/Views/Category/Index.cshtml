@model List<Category>
@{
    ViewData["Title"] = "Category";
}
<div class="card shadow border-0 mb-5 mt-5">
    <div class="card-header bg-primary bg-gradient ml-0">
        <div class="row">
            <div class="col-4"></div>
            <div class="col-lg-4 col-sm-6 text-center pt-2">
                <h3 class="text-white">Category List</h3>
            </div>
            @*            <div class="col-4 col-sm-4">
            <form method="post">
            @using (Html.BeginForm("Search", "Category", FormMethod.Post))
            {
            <div class="form-outline" data-mdb-input-init>
            <input class="rounded-bottom-3 px-2 bg-light" type="search" name="query" placeholder="Search category" id="form1" aria-label="Search" />
            <input asp-controller="Category" asp-action="Search" type="submit" value="Search" />
            </div>
            }
            </form>
            </div>*@
            <div class="col-lg-4 col-sm-6 text-end pt-2">
                <a asp-controller="Category" asp-action="Create" class="btn btn-dark">
                    <i class="bi bi-plus-circle"></i>  Create Category
                </a>
            </div>
            @* <div class="row">
            <div class="col-10 text-center">

            </div>
            <div class="col-2 text-center">
            <button type="button" class="btn btn-light dropdown-toggle" data-bs-toggle="dropdown">Sort</button>
            <ul class="dropdown-menu" aria-labelledby="sortDropdown">
            <li><a class="dropdown-item" href="@Url.Action("Index", new { sortOrder = "asc" })">sort by name [A-Z]</a></li>
            <li><a class="dropdown-item" href="@Url.Action("Index", new { sortOrder = "desc" })">sort by name [Z-A]</a></li>
            <li><a class="dropdown-item" href="@Url.Action("Index",new {sortOrder="pair"})">sort by display order [0-N]</a></li>
            <li><a class="dropdown-item" href="@Url.Action("Index", new {sortOrder="o"})">sort by display order [N-0]</a></li>
            </ul>
            </div>*@
        </div>
    </div>

    <div class="card-body">
        <table id="tblData" class="table table-bordered table-striped table-hover" style="width:100%">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Display Order</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            @*<tbody>
            @foreach (var row in Model)
            {
            <tr>
            <td class="custom-medium-text">@row.Name</td>
            <td class="custom-medium-text">@row.DisplayOrder</td>
            <td>
            <div class="w-75 btn-group" role="group">
            <a asp-area="Admin" asp-controller="Category" asp-action="Edit" asp-route-id="@row.Id" class="btn btn-primary mx-2 custom-medium-text">
            <i class="bi bi-pencil-square"></i> Edit
            </a>
            </div>
            </td>
            <td>
            <div class="w-75 btn-group" role="group">
            <a asp-area="Admin" asp-controller="Category" asp-action="Delete" asp-route-id="@row.Id" class="btn btn-danger mx-2">
            <i class="bi bi-trash-fill"></i> Delete
            </a>
            </div>
            </td>
            </tr>
            }
            </tbody>*@
        </table>
    </div>
</div>
@section Scripts
    {
    <script src="~/js/category.js"></script>
}